---
- name: Recursively change ownership of tomcat directory
  ansible.builtin.file:
    path: /opt/tomcat
    state: directory
    recurse: yes
    owner: "{{ run.user }}"
    group: "{{ run.user }}"

