---
- name: Download war file
  ansible.builtin.get_url:
    url: "{{ app.link }}"
    dest: /opt/tomcat/webapps

- name: Rename hmdm war file to root.war
  shell: "mv /opt/tomcat/webapps/{{ app.version}}.war /opt/tomcat/webapps/ROOT.war"